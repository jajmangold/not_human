`vision_latency.csv` is transcribed by hand from docs/lab-notebook/03-perception-latency.md
(single-frame latency, one GPU, no batching / ROI / CUDA graphs). Everything else plotted
in `figures/` is read directly from `speech/eval/*.json` and `speech/eval/results/`.
Regenerate figures with `python tools/make_figures.py`.
